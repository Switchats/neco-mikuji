# neco-mikuji(猫の気まぐれおみくじ)

ボタンを押すと猫の気まぐれな気分をランダムに占ってくれる、ちょっとしたおみくじアプリです。
Claude で生成した React コンポーネントを、ビルド不要の単一 HTML(React + Babel を CDN 読み込み)に変換したものです。

## 使い方

`index.html` をブラウザで直接開くだけで動作します。

## 元ファイル

`cat_mood_generator.jsx` はオリジナルの React コンポーネント(Vite などのビルド環境で使う場合の参考用)です。
